<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miaad.lesson8">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/quizicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/quizicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".QuizActivity" />
        <activity android:name=".QuizMenuActivity" />
        <activity android:name=".QuizHelpActivity" />
        <activity android:name=".QuizScoresActivity" />
        <activity android:name=".QuizSettingsActivity" />
        <activity android:name=".QuizGameActivity"></activity>
    </application>

</manifest>